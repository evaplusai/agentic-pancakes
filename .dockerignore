# Dependencies - node_modules excluded but lock files needed for npm ci
node_modules/
npm-debug.log
yarn-error.log
yarn.lock
# Note: package-lock.json is REQUIRED for npm ci

# Build outputs
dist/
*.tsbuildinfo

# Environment files
.env
.env.local
.env.*.local

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Git files
.git/
.gitignore
.gitattributes

# Testing
tests/
coverage/
*.test.ts
*.spec.ts
jest.config.js

# Documentation
*.md
!README.md
docs/

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml

# Docker files (don't copy into image)
Dockerfile
docker-compose.yml
.dockerignore

# Logs (will be mounted as volume)
logs/
*.log

# Temporary files
tmp/
temp/
*.tmp

# Scripts (not needed in production)
scripts/

# Note: src/ and tsconfig.json are REQUIRED for the builder stage
# They were previously ignored but are needed for multi-stage Docker builds

# Linting and formatting
.eslintrc*
.prettierrc*
.editorconfig

# Other
*.pid
*.seed
*.pid.lock
.cache
